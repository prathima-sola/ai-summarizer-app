# Briefly frontend

The client uses React, TypeScript, and Vite.

```bash
npm ci
npm run dev
npm test
npm run build
```

Set `VITE_API_URL` when the API does not run at `http://localhost:3001` during local development.
